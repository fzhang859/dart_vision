/**:
  ros__parameters:
    device: /dev/ttyACM0
    baud_rate: 115200
    read_timeout_ms: 100
    reconnect_interval_ms: 1000

    # 协议整数值 = ROS物理量 × scale
    yaw_scale: 1000.0
    distance_scale: 1000.0
    offset_scale: 1000.0

    send_topic: aim_command
    receive_topic: controller_state
